# API server URL — only needed if you're running web standalone (not behind nginx)
# In the monorepo dev setup, leave this blank — Vite proxies /api to localhost:3001
VITE_API_URL=https://api-production-9c68.up.railway.app
