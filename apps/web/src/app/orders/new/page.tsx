import { PageHeader } from "@/components/ui";

export default function Page() {
  return (
    <PageHeader
      icon="➕"
      title="New Indent (Call Desk)"
      subtitle="Create a new indent on behalf of a dealer"
    />
  );
}
