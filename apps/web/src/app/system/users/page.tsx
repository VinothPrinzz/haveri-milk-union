import { PageHeader } from "@/components/ui";

export default function Page() {
  return (
    <PageHeader
      icon="👤"
      title="User Management"
      subtitle="Manage admin users and access credentials"
    />
  );
}
