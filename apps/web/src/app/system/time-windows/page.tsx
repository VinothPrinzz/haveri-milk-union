import { PageHeader } from "@/components/ui";

export default function Page() {
  return (
    <PageHeader
      icon="⏰"
      title="Time Windows"
      subtitle="Configure ordering windows per zone"
    />
  );
}
