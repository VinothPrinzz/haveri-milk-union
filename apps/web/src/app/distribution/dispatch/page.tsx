import { PageHeader } from "@/components/ui";

export default function Page() {
  return (
    <PageHeader
      icon="📋"
      title="Daily Dispatch Sheet"
      subtitle="Today's dispatch routes and assignments"
    />
  );
}
