# Generated native projects — rebuilt by `expo prebuild` on every EAS build
android/
ios/

# Expo
.expo/
dist/
web-build/

# Local env
.env
.env.local
.env.*.local

# Google Play service-account key for `eas submit` — NEVER commit this
google-play-key.json
